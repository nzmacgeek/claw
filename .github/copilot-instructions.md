# Project Guidelines

## Build And Packaging
- For complete builds, update the project build number before building.
- When producing a dimsim package from a complete build, include that updated build number in the package version.
- Do not update the build number for syntax-only checks, single-file compilation, or other partial validation runs.